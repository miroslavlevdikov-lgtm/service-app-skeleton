<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--[COMMON][ACCESS_NETWORK_STATE]-->
    <!--[FIREBASE][ACCESS_POST_NOTIFICATIONS]-->
    <!--[APPSFLYER][AD_ID]-->

    <application
        android:name=".ServiceApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ServiceSkeleton"
        tools:replace="android:fullBackupContent">

        <!--[FIREBASE][koin_workmanager_provider]-->

        <!--[FIREBASE][meta_data]-->

        <!--[FIREBASE][messaging_service]-->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="screenLayout|orientation|screenSize
|keyboard|keyboardHidden|smallestScreenSize"
            android:label="@string/app_name"
            android:theme="@style/Theme.ServiceSkeleton">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--[APPSFLYER][intent_filter_oneclick]-->

        </activity>
    </application>

</manifest>